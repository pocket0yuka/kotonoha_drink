<%= turbo_frame_tag "private_posts" do %>
  <h1 class="text-lg p-6 text-center font-bold">非公開投稿</h1>
  <div class="flex flex-col items-center justify-center">
    <div class="overflow-x-auto h-96 w-full">
      <table class="table w-full">
        <tbody>
          <% private_posts.each do |post| %>
            <tr class="hover">
              <td class="relative space-y-4">
                <div class="flex flex-col justify-between h-full">
                  <div>
                    <!-- タグと画像、本文を含む部分 -->
                    <div class="flex flex-wrap">
                      <% post.tags.each do |tag| %>
                        <span class="badge badge-primary badge-outline mr-2">
                          <%= tag.name %>
                        </span>
                      <% end %>
                    </div>
                    <div class="my-4 flex justify-center">
                      <% if post.image.present? %>
                        <%= image_tag post.image.url, alt: "#{post.image}の画像", class: "w-50 h-50 object-cover rounded-lg" %>
                      <% end %>
                    </div>
                    <div class="p-2">
                      <%= post.body %>
                    </div>
                  </div>
                  <!-- 削除ボタンの配置 -->
                  <div class="mt-4 p-2">
                    <%= link_to post_path(post), method: :delete, data: { confirm: '本当に削除しますか？' }, class: "btn btn-outline btn-sm flex items-center absolute bottom-1 right-1" do %>
                      <%= l post.created_at, format: :short %>
                      <i class="fas fa-trash-alt ml-2"></i>
                    <% end %>
                  </div>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
<% end %>